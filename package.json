{
  "dependencies": {
    "fastify": "^4.28.1",
    "typescript": "^5.5.4"
  },
  "scripts": {
    "dev": "ts-node-dev --transpile-only --respawn --ignore-watch nod_modules src/http/server.ts"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0"
  }
}
